# Emacs Prelude Personal Configuration
My personal configuration of emacs prelude

## Configuration

```
$ ./install.sh
$ rm -rf ~/.emacs.d/personal
$ ln -s ~/emacs-prelude-personal ~/.emacs.d/personal
```
